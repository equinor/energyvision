import { create } from '@storybook/theming'

const theme = create({
  base: 'light',
  brandTitle: 'Energy Vision',
  brandImage: 'https://eds-static.equinor.com/logo/equinor-logo-horizontal.svg#red',
})

export { theme }
