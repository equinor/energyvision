apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: energyvision
spec:
  environments:
    - name: dev
      build:
        from: radix
  components:
    - name: web
      publicPort: http
      image: ghcr.io/equinor/energyvision/web:{imageTagName}
      alwaysPullImageOnDeploy: true
      ports:
        - name: http
          port: 3000
      environmentConfig:
        - environment: dev
          imageName: radix-latest
  dnsAppAlias:
    environment: dev
    component: web
  privateImageHubs:
    ghcr.io:
      username: nilsml
      email: nilsml@nilsland.com
